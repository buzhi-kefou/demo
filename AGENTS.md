# 会话记录与知识整理 Agent 规范

本文件用于指导将日常对话记录并整理为 Obsidian 可用的 Markdown 知识库。

## 一、目标

1. 持续记录每次问答。
2. 按主题定期整理，生成可检索的知识笔记。

## 二、全局原则

- 语言：中文为主，专业术语保留英文并注中文说明。
- 格式：Obsidian Markdown。
- 可读性：便于以后通过 Backlinks、标签和检索查看。

## 三、触发条件与记录对象

- 用户向助手提问或发出指令后，生成或修改了可落地的内容时，视为一次可记录事件。
- 如果助手回复中包含代码、命令、步骤、方案、流程、结论或明确可操作的结果，应在当前轮次结束后写入记录。
- 每次会话可包含多条问题与回答，按提问顺序编号。

## 四、目录约定（强制）

- 原始记录：`Inbox/<YYYY-MM-DD>.md`
- 整理笔记：`Notes/<主题>.md`
- 若目录不存在，需先创建。

## 三、对话记录格式

每次会话结束后，按以下结构写入：

```markdown
# YYYY-MM-DD 会话记录

## 1. 问题：xxx
**用户**：<用户原提问>
**助手**：<回答内容>

## 2. 问题：yyy
...
```

## 四、分类与标签

- 每条记录附加标签：`#提问记录`。
- 根据主题推荐附加标签，例如：
  - `#obsidian` / `#知识库` / `#笔记整理`
  - `#python` / `#git` / `#运维`

## 五、定期整理流程

每完成一个阶段的整理，依次执行以下操作：

1. 读取相关原始记录。
2. 提炼相同或相近主题。
3. 写入新笔记：`Inbox/<主题>.md`。
4. 对内容进行统一化、摘要化、术语核对。
5. 做一次回读校验，确认整理后有偏差、丢失上下文或保留未处理内容。

整理笔记应体现：
- 主题归纳
- 关键概念
- 相关提问链接

## 六、目录约定（建议）

- 原始记录：`Inbox/YYYY-MM-DD.md`
- 整理笔记：`Notes/<主题>.md`
